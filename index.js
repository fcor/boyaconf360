import {
  AppRegistry,
} from 'react-360';
import Home from './components/Home';

AppRegistry.registerComponent('slides_react360', () => slides_react360);
AppRegistry.registerComponent('home', () => Home);
